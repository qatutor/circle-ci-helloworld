version: 2
jobs:
  build:
    docker:
      - image: gcc:8.1.0
    working_directory: ~/work
    steps:
      - checkout
      - run:
          name: create report folder
          command: |
             mkdir -p ~/work/cucumber
             pwd
             ls
             touch ~/work/cucumber/report.txt
             echo "my testing notes" > ~/work/cucumber/report.txt
             pwd
             ls
            

      - store_artifacts:
          path: ~/work/cucumber
          $thispath = that is my variable
          echo $thispath
      - run:
              name: Send text report
              command: > 
                 curl -X POST -H 'Content-type: application/json' --data 
                 '{"text":"This is a link https://circleci.com/api/v1.1/project/:vcs-type/:username/:project/:build_num/artifacts"}' 
                 https://hooks.slack.com/services/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